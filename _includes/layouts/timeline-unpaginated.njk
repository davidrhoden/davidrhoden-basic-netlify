---
layout: layouts/base.njk
section: timeline-unpaginated
---

<article class="flex flex-wrap">
{% for year, posts in posts | groupby("post.data.date.year") %}
  justYear: {{ date | justYear }},
  post.data.date.year: {{ post.data.date.year }},
  year: {{ year }}:
  {% for post in posts %}
    <div class="left">
      <a href="{{ post.url | url }}">
        {% if post.data.image %}
        <img class="tiny-thumbnail" src="{{ post.data.image }}?nf_resize=smartcrop&h=32&w=32">
        {% endif %}
      </a>
    </div>
  {% endfor %}
  {{ date | justYear }} year end
{% endfor %}
</article>