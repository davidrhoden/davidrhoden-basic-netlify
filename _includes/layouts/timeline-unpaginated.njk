---
layout: layouts/base.njk
section: timeline-unpaginated
---

<article class="flex flex-wrap">
{% for year, posts in posts | groupby("date.year.value") %}
  justYear: {{ date | justYear }},
  date.year: {{ date.year.value }},
  year: {{ year }}:
  {% for post in posts %}
    <div class="left">
      <a href="{{ post.url | url }}">
        {% if post.data.image %}
        <img class="tiny-thumbnail" src="{{ post.data.image }}?nf_resize=smartcrop&h=32&w=32">
        {% endif %}
      </a>
    </div>
  {% endfor %}
{% endfor %}
</article>